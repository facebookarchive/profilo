include_defs("//buck_imports/xplat")

def loom_path(dep):
    if THIS_IS_FBANDROID:
        root = "//libraries/loom/"
        if dep.startswith("deps/"):
            dep = "facebook/" + dep
    else:
        root = "//"

    result = "{root}{dep}".format(root=root, dep=dep)
    return result
